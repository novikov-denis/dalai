# Даль AI — ИИ-редактор для редакторов

Современный AI-инструмент для проверки текстов на соответствие редакционной политике с красивым интерфейсом в стиле "Живое Слово".

## ✨ Возможности

- 🤖 **AI-анализ текстов** — проверка на соответствие принципам редполитики
- 📝 **Markdown редактор** — поддержка форматирования и предпросмотр
- 🎨 **Интерактивные правки** — подсветка проблем прямо в тексте
- 💾 **История анализов** — автоматическое сохранение в localStorage
- 🎯 **Типизация правок** — style, tone, grammar, clarity
- ⚙️ **Настройки** — управление параметрами анализа
- 🎭 **Режим фокуса** — концентрация на тексте без отвлечений

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd dal-ai
npm install
```

### 2. Настройка переменных окружения

Файл `.env` уже создан с ключом NeuroAPI:

```env
NEUROAPI_API_KEY=sk-zeZG5EtVdQXQAHL1gL27oq2EYJR3NosF8OV2kS6HjCu41ztv
NEUROAPI_BASE_URL=https://neuroapi.host/v1
```

### 3. Запуск проекта

```bash
# Запуск в режиме разработки
npm run dev

# Или запустить клиент и сервер отдельно
npm run dev:client  # Клиент на порту 5000
npm run dev         # Сервер на порту 5001
```

Проект будет доступен по адресу: **http://localhost:5000**

## 🏗️ Структура проекта

```
dal-ai/
├── client/               # React Frontend (Vite + TypeScript)
│   └── src/
│       ├── components/   # UI компоненты
│       │   ├── editor.tsx           # Главный редактор
│       │   ├── history-dialog.tsx   # История анализов
│       │   ├── settings-dialog.tsx  # Настройки
│       │   ├── suggestion-card.tsx  # Карточка предложения
│       │   └── ui/                  # Radix UI компоненты
│       ├── pages/
│       │   └── home.tsx            # Главная страница
│       └── lib/
│           └── utils.ts            # Утилиты
├── server/               # Express Backend
│   ├── routes.ts        # API endpoints (/api/analyze)
│   └── index-dev.ts     # Dev сервер
└── shared/
    └── schema.ts        # Общие типы и схемы
```

## 🎨 Дизайн

Интерфейс выполнен в стиле "Живое Слово" с акцентом на:
- 📖 **Serif шрифты** для заголовков
- 🎨 **Приглушенная палитра** — бежевые и кремовые тона
- ✒️ **Типографика** — внимание к деталям текста
- 🎭 **Анимации** — плавные переходы с Framer Motion

### Цветовая схема:
- **Primary**: Темно-коричневый/сепия
- **Accent**: Янтарный/медовый
- **Background**: Кремовый/пергамент (#FDFBF7)

## 🔧 API Endpoints

### POST `/api/analyze`
Анализ текста на соответствие редакционной политике

**Request:**
```json
{
  "text": "Ваш текст для проверки..."
}
```

**Response:**
```json
{
  "suggestions": [
    {
      "id": "1",
      "original": "Данный функционал",
      "replacement": "Эта функция",
      "reason": "Канцелярит. Живая речь требует простых глаголов.",
      "type": "style",
      "status": "pending"
    }
  ]
}
```

## 📚 Принципы редакционной политики

Инструмент проверяет тексты по следующим принципам:

1. **ЧЕСТНОСТЬ** — без обмана и завышенных обещаний
2. **СПОКОЙСТВИЕ** — без излишнего восторга и давления
3. **ЭМПАТИЯ** — поддержка студента, без дискриминации
4. **КОНКРЕТНОСТЬ** — ёмко и по делу
5. **ПОНЯТНОСТЬ** — язык студента, без канцелярита
6. **ПРАКТИЧНОСТЬ** — примеры вместо определений

## 💾 Хранение данных

- **История** сохраняется в `localStorage` браузера
- **Ключ**: `dal-ai-history`
- **Формат**: JSON массив с последними 30 анализами
- Можно очистить через Settings → Данные → Очистить историю

## 🎯 Типы правок

- **style** — стилистические замечания (канцелярит, штампы)
- **tone** — тон и голос (спокойствие, эмпатия)
- **grammar** — грамматические ошибки
- **clarity** — ясность и понятность

## ⌨️ Горячие клавиши

- **Ctrl/Cmd + Enter** — запустить анализ
- **Esc** — выход из режима фокуса
- **Tab** — принять текущую правку (в разработке)

## 🛠️ Технологии

**Frontend:**
- React 19
- TypeScript
- Vite
- Tailwind CSS
- Radix UI
- Framer Motion
- React Query
- Wouter (routing)

**Backend:**
- Express
- TypeScript
- OpenAI SDK (для NeuroAPI)
- Zod (валидация)

## 📦 Команды

```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен версии
npm run check        # Проверка типов TypeScript
npm run db:push      # Обновление схемы БД (Drizzle)
```

## 🌟 Фичи в разработке

- [ ] Авторизация пользователей
- [ ] Сохранение истории в БД
- [ ] Экспорт в Markdown/HTML
- [ ] Режим совместной работы
- [ ] Интеграция с Google Docs
- [ ] Кастомные правила проверки
- [ ] API для внешних сервисов

## 🐛 Известные проблемы

- Подсветка текста работает только для коротких фрагментов
- История ограничена 30 записями
- Нет поддержки офлайн-режима

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Создано с ❤️ для редакторов

---

**Даль AI** — потому что язык должен быть живым!
