# Даль AI — Быстрый старт

## 🚀 Запуск проекта

### Способ 1: Автоматический запуск (рекомендуется)

```bash
cd /Users/novikov-denis/Desktop/vladimir-dal-ai/dal-ai
./start.sh
```

Скрипт автоматически запустит:
- 📦 Vite dev server на `http://localhost:5000` (фронтенд)
- ⚙️ Express API server на `http://localhost:5001` (бэкенд)

### Способ 2: Ручной запуск

#### Терминал 1: Запуск клиента
```bash
cd /Users/novikov-denis/Desktop/vladimir-dal-ai/dal-ai
npm run dev:client
```

#### Терминал 2: Запуск сервера
```bash
cd /Users/novikov-denis/Desktop/vladimir-dal-ai/dal-ai
npm run dev
```

## 🔑 Настройка API ключа

Файл `.env` уже настроен с рабочим ключом NeuroAPI:

```env
NEUROAPI_API_KEY=sk-zeZG5EtVdQXQAHL1gL27oq2EYJR3NosF8OV2kS6HjCu41ztv
NEUROAPI_BASE_URL=https://neuroapi.host/v1
```

## 📱 Доступ к приложению

После запуска откройте в браузере:
- **Приложение**: http://localhost:5000
- **API**: http://localhost:5001

## 🧪 Тестирование API

```bash
curl -X POST http://localhost:5001/api/analyze \
  -H "Content-Type: application/json" \
  -d '{"text": "Данный функционал позволяет пользователю осуществлять проверку текста."}'
```

Ожидаемый ответ:
```json
{
  "suggestions": [
    {
      "id": "1",
      "original": "Данный функционал позволяет пользователю",
      "replacement": "Эта функция помогает",
      "reason": "Канцелярит. Живая речь требует простых глаголов.",
      "type": "style",
      "status": "pending"
    }
  ]
}
```

## 🛠️ Доступные команды

```bash
npm run dev:client  # Только клиент (Vite)
npm run dev         # Только сервер (Express)
npm run build       # Сборка для продакшена
npm run start       # Запуск продакшен версии
npm run check       # Проверка типов TypeScript
```

## 🎯 Использование

1. **Откройте приложение** в браузере: http://localhost:5000
2. **Введите текст** в редактор или вставьте готовый
3. **Нажмите "Проверить слог"** — AI проанализирует текст
4. **Просмотрите правки** в правой панели
5. **Примите или отклоните** каждое предложение
6. **Сохраните результат** — он автоматически добавится в историю

## 📚 Возможности

- ✅ AI-анализ текстов по редакционной политике
- ✅ Markdown редактор с предпросмотром
- ✅ Интерактивная подсветка проблемных мест
- ✅ История анализов (localStorage)
- ✅ Режим фокуса для концентрации
- ✅ Настройки параметров анализа

## 🐛 Устранение проблем

### Ошибка "Cannot find module"
```bash
cd /Users/novikov-denis/Desktop/vladimir-dal-ai/dal-ai
npm install
```

### Порт занят
Измените порт в `vite.config.ts` или остановите другие процессы:
```bash
lsof -i :5000
kill -9 <PID>
```

### API не отвечает
Проверьте, что Express сервер запущен:
```bash
curl http://localhost:5001/api/health
```

Если 404, проверьте логи сервера или перезапустите:
```bash
npm run dev
```

## 📦 Структура проекта

```
dal-ai/
├── client/              # React Frontend
│   ├── src/
│   │   ├── components/ # UI компоненты
│   │   ├── pages/      # Страницы
│   │   └── lib/        # Утилиты
├── server/              # Express Backend
│   ├── routes.ts       # API endpoints
│   └── index-dev.ts    # Dev сервер
├── shared/              # Общие типы
├── .env                 # Переменные окружения
├── start.sh            # Скрипт автозапуска
└── README.md           # Документация
```

## 🎨 Технологии

- **Frontend**: React 19, TypeScript, Vite, Tailwind CSS, Radix UI, Framer Motion
- **Backend**: Express, TypeScript, OpenAI SDK (NeuroAPI)
- **Деплой**: Vercel, Railway, или любой Node.js хостинг

## 📖 Дополнительная документация

- [README.md](./README.md) — полная документация
- [DESIGN_PROMPT.md](../DESIGN_PROMPT.md) — описание дизайна старой версии
- [Техническое задание](./attached_assets/) — требования к редизайну

---

**Даль AI** — живое слово в каждом тексте! ✍️
